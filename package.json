{
  "name": "TaskManagerAPI",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "npm install && ts-node-dev -r tsconfig-paths/register --respawn --transpile-only --igonre-watch node_modules --no-notify --inspect -- ./src/server.ts",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "nodejs",
    "typescript",
    "mongoDb",
    "docker"
  ],
  "author": "Marcelo Peres",
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^20.2.5",
    "ts-node-dev": "^2.0.0",
    "tsconfig-paths": "^4.2.0",
    "typescript": "^5.0.4"
  }
}
